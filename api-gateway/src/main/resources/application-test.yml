# ============================================
# Test 환경
# ============================================

service-endpoints:
  member: ${MEMBER_SERVICE_URL:http://localhost:8081}
  product: ${PRODUCT_SERVICE_URL:http://localhost:8082}
  market: ${MARKET_SERVICE_URL:http://localhost:8083}
  payment: ${PAYMENT_SERVICE_URL:http://localhost:8084}
  settlement: ${SETTLEMENT_SERVICE_URL:http://localhost:8085}

cors:
  allowed.origins: http://localhost:3000,http://localhost:8080

jwt:
  secret: thock-gateway-test-jwt-secret-key-which-is-long-enough-32chars
  access-token-exp-seconds: 1800
  refresh-token-exp-seconds: 1209600
  issuer: "thock-test"

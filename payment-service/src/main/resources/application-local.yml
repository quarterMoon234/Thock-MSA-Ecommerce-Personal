spring:

  kafka:
    bootstrap-servers: localhost:9092

  datasource:
    url: jdbc:mysql://localhost:3307/thock_payment_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root

# 내부 백엔드 URL (Local)
custom:
  global:
    internalBackUrl: http://localhost:8080

swagger:
  server:
    url: http://localhost:8080
    description: Local API Gateway